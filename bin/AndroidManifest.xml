<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mindful.music"
    android:versionCode="1"
    android:versionName="1.0" >
 <uses-permission android:name="android.permission.CAMERA" />
 <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
 <uses-permission android:name="android.permission.RECORD_AUDIO" />
     <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="18" />
   <uses-feature android:name="android.hardware.camera"
                  android:required="true" />
     
 <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
     <activity android:label="@string/app_name" android:name="RecordActivity"/>
     <activity android:label="@string/app_name" android:name="MemoryActivity"/>
     <activity android:label="@string/app_name" android:name="RecordAudioActivity"/>
     <activity android:label="@string/app_name" android:name="AutoPlayActivity"/>
     <activity android:label="@string/app_name" android:name="FamilyTab"/>
     <activity android:label="@string/app_name" android:name="ObjectsTab"/>
     <activity android:label="@string/app_name" android:name="OthersTab"/>
        <activity
            android:name="com.mindful.music.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
               <action android:name="android.intent.action.MAIN" />
               <category android:name="android.intent.category.LAUNCHER" />
           </intent-filter>    
        </activity>
    </application>

</manifest>
